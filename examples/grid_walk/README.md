# Grid Walk Demo

This example is implemented in `src/renee/examples/grid_walk.py` and exposed via the CLI:

```bash
renee demo local --renderer terminal
renee demo server
renee demo client
```

If you have pygame installed:

```bash
renee demo local --renderer pygame
```

