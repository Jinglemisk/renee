"""Command modules for the Renee CLI.

This package contains command implementations organized by functionality.
Commands can be split into separate modules as the CLI grows.

Example structure:
- project.py - Project management commands (new, validate)
- game.py - Game execution commands (run, simulate)
- introspection.py - Introspection commands (schema, query)
- repl.py - REPL command
"""

# This package is currently a placeholder for future command organization.
# As the CLI grows, commands from main.py can be refactored into separate
# modules here for better organization.
